<Application x:Class="WpfApp1.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WpfApp1"
             StartupUri="LoginWindow.xaml">
    <Application.Resources>
        <!-- Основной фон -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#FFB3BBFF"/>

        <!-- Темнее на 20% для кнопок -->
        <SolidColorBrush x:Key="ButtonBrush" Color="#FF828BD3"/>

        <!-- При наведении ещё темнее -->
        <SolidColorBrush x:Key="ButtonHoverBrush" Color="#FF6C7DBE"/>

        <!-- Общий стиль для всех кнопок -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource ButtonBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Height" Value="30"/>
            <!-- Добавлено: фиксированная высота -->
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource ButtonHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{StaticResource ButtonHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Градиентный фон для всех окон -->
        <Style TargetType="Window">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                        <GradientStop Color="#FF8892E6" Offset="0"/>
                        <GradientStop Color="#FFB1B7ED" Offset="0.5"/>
                        <GradientStop Color="#FFB3BBFF" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
        </Style>

        <!--Выравнивание текста в полях по вертикали-->
            <Style TargetType="TextBox">
            <Setter Property="VerticalContentAlignment" Value="Center">
            </Setter>
        </Style>
        
    </Application.Resources>
</Application>
