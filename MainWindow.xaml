<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Style="{StaticResource {x:Type Window}}"
        Title="Регистрация" Height="450" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="Регистрация"
                   FontSize="30"
                   Foreground="White" 
                   FontWeight="Bold"
                   Margin="10"
                   HorizontalAlignment="Center"
                   Grid.Row="0"
                   Grid.Column="1" />

        <!-- Email -->
        <Grid Grid.Row="1" Grid.Column="1" Margin="0,5,0,10">
            <TextBox x:Name="EmailTextBox" Width="150" Height="25"/>
            <TextBlock x:Name="EmailTextBlock" Text="Электронная почта" Foreground="Gray"
                       IsHitTestVisible="False" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="25,5,0,0"/>
        </Grid>

        <!-- Полное имя -->
        <Grid Grid.Row="2" Grid.Column="1" Margin="0,0,0,10">
            <TextBox x:Name="FullNameTextBox" Width="150" Height="25"/>
            <TextBlock x:Name="FullNameTextBlock" Text="Полное имя" Foreground="Gray"
                       IsHitTestVisible="False" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="25,0,0,0"/>
        </Grid>

        <!-- Пароль аккаунта -->
        <Grid Grid.Row="3" Grid.Column="1" Margin="0,0,0,10">
            <TextBox x:Name="AccountPasswordTextBox" Width="150" Height="25"/>
            <TextBlock x:Name="AccountPasswordTextBlock" Text="Пароль для аккаунта" Foreground="Gray"
                       IsHitTestVisible="False" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="25,0,0,0"/>
        </Grid>

        <!-- Повтор пароля аккаунта -->
        <Grid Grid.Row="4" Grid.Column="1" Margin="0,0,0,10">
            <TextBox x:Name="RepeatAccountPasswordTextBox" Width="150" Height="25"/>
            <TextBlock x:Name="RepeatAccountPasswordTextBlock" Text="Повторите пароль" Foreground="Gray"
                       IsHitTestVisible="False" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="25,0,0,0"/>
        </Grid>

        <!-- Ввод кода подтверждения -->
        <Grid x:Name="VerificationCodeGrid" Grid.Row="5" Grid.Column="1" Margin="0,0,0,10" Visibility="Collapsed">
            <TextBox x:Name="VerificationCodeTextBox" Width="150" Height="25"/>
            <TextBlock x:Name="VerificationCodeTextBlock" Text="Код подтверждения" Foreground="Gray"
                       IsHitTestVisible="False" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0,0,0"/>
        </Grid>

        <!-- Кнопки -->
        <StackPanel Grid.Row="6" Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Center">
            <Button x:Name="RegisterButton" Content="Зарегистрироваться"
                    Width="150" Height="30" Margin="0,5"/>
            <Button x:Name="ConfirmButton" Content="Подтвердить"
                    Width="150" Height="30" Margin="0,5" Visibility="Collapsed"/>
        </StackPanel>

        <!-- Ссылка для перехода к входу -->
        <TextBlock Grid.Row="6" Grid.Column="1" HorizontalAlignment="Center" Margin="0,130,0,10" FontSize="12" TextAlignment="Center">
            <Hyperlink Click="LoginHyperlink_Click" Foreground="Blue" Cursor="Hand" TextDecorations="Underline">
                Используете YAbank не впервые?
                <LineBreak/>
                Войти в аккаунт
            </Hyperlink>
        </TextBlock>
    </Grid>
</Window>
